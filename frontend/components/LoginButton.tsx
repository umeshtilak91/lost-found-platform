export default function LoginButton() {
  return (
    <button
      style={{
        backgroundColor: "#2563eb",
        color: "white",
        padding: "12px 20px",
        border: "none",
        borderRadius: "6px",
        cursor: "pointer",
        marginTop: "20px",
      }}
    >
      Login with Google (Coming Soon)
    </button>
  );
}